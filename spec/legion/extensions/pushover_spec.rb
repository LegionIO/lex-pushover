RSpec.describe Legion::Extensions::Pushover do
  it 'has a version number' do
    expect(Legion::Extensions::Pushover::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
